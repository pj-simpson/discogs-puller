version: '3.7'

services:

  starlite:
    build:
      context: .
      dockerfile: Dockerfile
    command: /start
    volumes:
      - .:/home/myuser/code/
    ports:
      - "8000:8000"
    env_file:
      - .env
